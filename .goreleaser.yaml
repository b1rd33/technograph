version: 2

project_name: technograph

metadata:
  mod_timestamp: "{{ .CommitTimestamp }}"

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - id: technograph
    main: ./cmd/technograph
    binary: technograph
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - >-
        -s -w
        -X github.com/b1rd33/technograph/internal/buildinfo.Version={{ .Version }}
        -X github.com/b1rd33/technograph/internal/buildinfo.Commit={{ .ShortCommit }}
        -X github.com/b1rd33/technograph/internal/buildinfo.Date={{ .Date }}
  - id: technograph-mcp
    main: ./cmd/technograph-mcp
    binary: technograph-mcp
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - >-
        -s -w
        -X github.com/b1rd33/technograph/internal/buildinfo.Version={{ .Version }}
        -X github.com/b1rd33/technograph/internal/buildinfo.Commit={{ .ShortCommit }}
        -X github.com/b1rd33/technograph/internal/buildinfo.Date={{ .Date }}

archives:
  - id: technograph
    ids:
      - technograph
      - technograph-mcp
    formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else }}{{ .Arch }}{{ end }}
    files:
      - LICENSE
      - README.md

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  github:
    owner: b1rd33
    name: technograph
  draft: false
  prerelease: auto
