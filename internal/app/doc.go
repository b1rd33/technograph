// Package app exposes the reusable technograph scanning application.
//
// User interfaces such as the legacy CLI, future structured CLI commands, and
// protocol adapters should depend on this package instead of constructing
// probes and the fingerprint engine themselves.
package app
